# ระบบเว็บชุมชนท่าสาป (thasap-web)

เอกสารสรุปสถาปัตยกรรมและสถานะปัจจุบันของระบบ อัปเดตล่าสุด: 2026-07-23

## ภาพรวม

เว็บไซต์ประชาสัมพันธ์ชุมชนตำบลท่าสาป อ.เมือง จ.ยะลา — นำเสนอสินค้าชุมชน สถานที่ท่องเที่ยว ข่าวสาร และข้อมูล 6 หมู่บ้าน พร้อมระบบหลังบ้านให้สมาชิกชุมชน/แอดมินกรอกข้อมูลเอง

## Stack เทคโนโลยี

- **Backend**: Laravel 12 (PHP 8.2+)
- **Admin panel**: Filament v5 (`/admin`)
- **Database**: MySQL (`thasap_web`)
- **สิทธิ์ผู้ใช้**: spatie/laravel-permission (roles: `super_admin`, `admin`)
- **Slug generation**: spatie/laravel-sluggable
- **Frontend**: Blade + Tailwind CSS (Vite build) — ไม่มี framework JS ฝั่ง client (ใช้ vanilla JS เฉพาะจุด เช่น hero slider, product gallery)
- **แผนที่**: Leaflet.js + OpenStreetMap (โหลดผ่าน CDN ไม่ต้องใช้ API key)
- **หลายภาษา**: ระบบแปล UI ในตัวของ Laravel (ไทย/มลายู)

## โครงสร้างข้อมูล (Database)

| ตาราง | หน้าที่ | ความสัมพันธ์ |
|---|---|---|
| `villages` | ข้อมูล 6 หมู่บ้าน (หมู่ที่, ชื่อ, ประวัติ, จุดเด่น, รูป, พิกัด GPS) | hasMany `attractions`, `products` |
| `attractions` | สถานที่ท่องเที่ยว (ชื่อ, ประเภท, เวลาเปิด-ปิด, วิธีเดินทาง, พิกัด GPS, ปักหมุดหน้าแรก) | belongsTo `village` |
| `products` | สินค้าชุมชน (ชื่อ, หมวดหมู่, ราคา, หน่วย, ติดต่อ, หมู่บ้านที่สังกัด, ปักหมุดหน้าแรก) | belongsTo `village`, hasMany `images` |
| `product_images` | รูปสินค้า **หลายรูปต่อชิ้น** พร้อม flag รูปหน้าปกและลำดับ | belongsTo `product` |
| `posts` | ข่าวสาร/กิจกรรม/ประกาศ | — |
| `hero_slides` | ภาพสไลด์หน้าแรก (รูป, หัวข้อทับรูป, ลำดับ, ระยะเวลาแสดง) | — |
| `users` | ผู้ใช้ระบบหลังบ้าน + roles (spatie/permission) | — |

ทุกตารางเนื้อหา (villages/attractions/products/posts) มี `is_active` (เปิด/ปิดการแสดงผล) และบันทึก `created_by` (เจ้าของข้อมูล เพื่อจำกัดสิทธิ์แก้ไข)

## หน้าเว็บสาธารณะ (29 routes)

| เส้นทาง | หน้าที่ |
|---|---|
| `/` | หน้าแรก — hero slideshow, สถิติ, อัตลักษณ์ชุมชน, สินค้า/สถานที่/ข่าวเด่น, 6 หมู่บ้าน, แผนที่ชุมชน |
| `/products`, `/products/{slug}` | รายการ + รายละเอียดสินค้า (กรองหมวดหมู่, แกลเลอรีหลายรูป) |
| `/attractions`, `/attractions/{slug}` | รายการ + รายละเอียดสถานที่ท่องเที่ยว (มีแผนที่ + ปุ่มนำทาง Google Maps) |
| `/villages`, `/villages/{slug}` | รายการ + รายละเอียดหมู่บ้าน |
| `/posts`, `/posts/{slug}` | ข่าวสาร (กรองหมวดหมู่ + เดือน) |
| `/map` | แผนที่ชุมชนรวม (หมุดสถานที่ + หมู่บ้าน/สินค้า) |
| `/lang/{locale}` | สลับภาษา (th/ms) |

## ระบบหลังบ้าน (Filament `/admin`)

**Resources**: สินค้า, สถานที่ท่องเที่ยว, ข่าวสาร, หมู่บ้าน, ภาพสไลด์หน้าแรก, จัดการผู้ใช้

**สิทธิ์การเข้าถึง**:
- `admin` — จัดการสินค้า/สถานที่/ข่าวสารของตัวเองได้เท่านั้น (ผ่าน `getEloquentQuery()` scope ตาม `created_by`)
- `super_admin` — เข้าถึงทุกอย่างรวมถึงหมู่บ้าน, ภาพสไลด์, จัดการผู้ใช้ (resource เหล่านี้ซ่อนจาก `admin` ทั้งหมดผ่าน `canAccess()`)

**ฟีเจอร์เด่นในฟอร์ม**:
- จัดกลุ่มฟิลด์เป็น Section มีไอคอน/คำอธิบาย responsive
- อัปโหลดรูปพร้อม image editor (ครอป/หมุน) และคำแนะนำขนาด/สัดส่วนต่อประเภทเนื้อหา
- สินค้า: อัปโหลดได้หลายรูป, ปุ่ม ⭐ ตั้งรูปหน้าปก (ย้ายขึ้นบนสุดอัตโนมัติ), badge บอกสถานะ
- Toggle "ปักหมุดหน้าแรก" (สินค้า/สถานที่)

## กลไกที่ไม่ตรงไปตรงมา (ควรรู้ก่อนแก้โค้ด)

1. **การเลือกสินค้า/สถานที่ขึ้นหน้าแรก** (`HomeController::pickHighlighted()`) — ดึงรายการที่ปักหมุด (`is_featured`) มาก่อน แล้ว **สุ่ม** เติมที่เหลือให้ครบ 3 จากรายการที่เหลือทุกครั้งที่โหลดหน้า เพื่อไม่ให้ใครได้เปรียบจากการอัปโหลดล่าสุด
2. **รูปหน้าปกสินค้า** (`ProductImage` model events) — เมื่อตั้งรูปใดเป็นปก ระบบจะปลดรูปปกเดิมอัตโนมัติผ่าน DB event (`saved`/`deleted`) แล้ว sync ค่าไปยัง `products.image` (คอลัมน์เดิมที่หน้า list/การ์ดต่างๆ ยังใช้อยู่) ผ่าน `Product::syncCoverImage()`
3. **แผนที่ชุมชน** — ใช้ partial ร่วม `resources/views/partials/community-map.blade.php` ระหว่างหน้าแรกกับหน้า `/map` หมุดมี 2 สี: เขียว = สถานที่ท่องเที่ยว (พิกัดเฉพาะจุด), ส้ม = หมู่บ้าน (พิกัดระดับหมู่บ้าน คลิกดูรายการสินค้าในหมู่บ้านนั้นได้)
4. **หลายภาษา** — แปลเฉพาะ "UI คงที่" (เมนู, ปุ่ม, หัวข้อ) ผ่าน `lang/th/site.php` และ `lang/ms/site.php` **เนื้อหาที่แอดมินกรอกเอง (ชื่อสินค้า, รายละเอียด, ข่าว) ยังเป็นภาษาไทยอย่างเดียว ไม่ถูกแปล**

## ข้อจำกัด/สิ่งที่ยังไม่ได้ทำ

- **ข้อมูลติดต่อใน footer เป็นค่าตัวอย่างชั่วคราว** (ที่อยู่, เบอร์โทร, อีเมล) — รอแอดมินแก้เป็นข้อมูลจริง (มี comment กำกับไว้ในโค้ด)
- **ยังไม่มีพิกัด GPS จริง** ในหมู่บ้าน/สถานที่ท่องเที่ยวส่วนใหญ่ — ต้องกรอกผ่าน `/admin` เพื่อให้แผนที่ขึ้นหมุด
- **แปลเฉพาะ UI ไม่แปลเนื้อหา** ตามข้อ 4 ด้านบน — ถ้าต้องการแปลเนื้อหาทุกชิ้นเป็นมลายูด้วยต้องเพิ่มคอลัมน์แยกภาษาในฐานข้อมูล (งานใหญ่ ต้องมีคนแปลต่อเนื่อง)
- **`Storage::url()` ใช้ disk เริ่มต้นแบบไม่ระบุชัดเจน** — ทำงานถูกต้องตอนนี้เพราะ fallback ของ Laravel บังเอิญตรงกับ path จริง แต่ถ้าเปลี่ยนค่า `FILESYSTEM_DISK` ใน `.env` ในอนาคต (เช่นย้ายไป cloud storage) รูปภาพทั้งเว็บอาจพังทันที
- ไม่มี test อัตโนมัติสำหรับฟีเจอร์ที่สร้างขึ้น (มีแต่ test ตัวอย่าง default ของ Laravel)
- README.md ยังเป็นค่า default ของ Laravel ไม่ได้อธิบายโปรเจค

## แนะนำขั้นตอนต่อไป (เรียงตามความสำคัญ)

1. **กรอกข้อมูลจริงแทนข้อมูลตัวอย่าง** — ปัจจุบันมีเนื้อหาทดสอบปนอยู่ (เช่น "ตัวอย่างภูเขา", "ตัวอย่างชื่อสถานที่") และ footer ยังเป็นที่อยู่/เบอร์โทร/อีเมลสมมติ ต้องแทนที่ด้วยข้อมูลจริงก่อนเปิดใช้งานจริง
2. **กรอกพิกัด GPS ของหมู่บ้าน/สถานที่ท่องเที่ยว** — เพื่อให้แผนที่หน้าแรกและ `/map` มีหมุดขึ้นจริง (ตอนนี้ว่างเปล่าเกือบทั้งหมด)
3. **วางแผนขึ้นเว็บจริง (deploy)** — ตอนนี้รันบน local (XAMPP/`php artisan serve`) เท่านั้น ต้องตัดสินใจเรื่อง hosting, โดเมน, SSL, และวิธี deploy/backup ก่อนเปิดให้สาธารณะเข้าถึง
4. **เขียน automated test อย่างน้อยสำหรับ logic ที่ซับซ้อน** — โดยเฉพาะระบบ sync รูปหน้าปกสินค้า และการเลือกสินค้า/สถานที่ขึ้นหน้าแรก (featured+random) เพราะเป็น custom logic ที่ถ้าพังจะสังเกตยาก
5. **แก้ `Storage::url()` ให้ระบุ disk ชัดเจน** — ป้องกันปัญหารูปภาพพังทั้งเว็บถ้ามีการเปลี่ยนค่า config ในอนาคต (ความเสี่ยงต่ำตอนนี้ แต่แก้ง่ายและควรทำก่อนขึ้นจริง)
6. **ตัดสินใจเรื่องแปลเนื้อหาเป็นมลายู** — ถ้าชุมชนต้องการแปลเนื้อหาที่แอดมินกรอกเอง (ไม่ใช่แค่ UI) ต้องวางแผนเรื่องคนแปลและโครงสร้างฐานข้อมูลเพิ่มเติม เป็นงานที่มีภาระต่อเนื่อง ไม่ใช่ทำครั้งเดียวจบ
7. **อัปเดต README.md** ให้อธิบายโปรเจคจริง (ตอนนี้ยังเป็นค่า default ของ Laravel) เพื่อให้คนอื่นที่มาดูแลต่อเข้าใจระบบง่ายขึ้น
